This is for checking if there are cards in local storage.
                if (all_cards == null ) {
                    localStorage.setItem("corkboard", JSON.stringify([{"slug": "INT DAY", "scene": "A writer creates their first card"}]))
                    Cards.newCard("INT DAY", "A writer creates their first card")
                    
                } else {
                    let data = JSON.parse(all_cards)
                    for (let i = 0;i<data.length; i++) {
                        Cards.newCard(data[i].slug, data[i].scene)
                    }
                    

                }


April 5.
   * Can create new cards. Can parti:ally draw cards. But when refreshing the page, the cards in the library dissapear. 
   Therer's a problem with having CardLibary getting set to an empty object. Somehow it's resetting everythign
   when reloading.


April 6
    The Draw card function draws all the cards. Even if  the cards are already drawn.:P
